import React from 'react';

interface EmptyComponentProps {
}

const NavBar: React.FC<EmptyComponentProps> = (props) => {
    return (
        <div>

            
            
        </div>
    );
};

export default NavBar;